#pragma kernel StrokeyQuads

Texture2D<float> _sourceTexture;

AppendStructuredBuffer<int2> _quadPoints;

[numthreads(32,32,1)]
void StrokeyQuads (uint3 id : SV_DispatchThreadID) {
  float color = _sourceTexture[id.xy];

  if (color > 0.5) {
    _quadPoints.Append(int2(id.xy));
  }
}
